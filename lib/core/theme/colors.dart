import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // private constructor

  static const Color white = Colors.white;
  static const Color black = Colors.black;

  static const Color primary = Color.fromARGB(255, 5, 5, 5);
  static const Color secondary = Color(0xFF4FD1C5);

  static const Color boderColor = Colors.grey;

  static const Color textFeildIcon = Color.fromARGB(255, 110, 110, 110);
  static const Color scaffoldBackground = Color(0xFFF7F7FB);

  static const Color textWhite = Colors.white;
  static const Color textBlack = Colors.white;
  static const Color textSecondary = Colors.grey;
}
